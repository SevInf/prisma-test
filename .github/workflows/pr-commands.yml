on:
  issue_comment:
    types:
      - created

jobs:
  check_commands:
    if: ${{ github.event.issue.pull_request && contains('/ci', github.event.comment.body) }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.comment.body }}"
